// src/pages/Preferences.tsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const Preferences: React.FC = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Preferences Page</Typography>
    </Box>
  );
};

export default Preferences;